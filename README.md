
  # Content Authenticity Verifier MVP

  This is a code bundle for Content Authenticity Verifier MVP. The original project is available at https://www.figma.com/design/H7xuyj4weTWCuOVt4SBx1w/Content-Authenticity-Verifier-MVP.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  